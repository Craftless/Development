#include<bits/stdc++.h>

using namespace std;

int main() {

    int numbers [4];
    string hi = "hello";
    //cout << "Enter numbers separated by a comma, a space or a line break" << flush;
    ///cin >> hi;
    /*
    for (int i = 0; i < hi.length(); i++) {
        if (stoi(hi[i])) {

        }
    }
    */

    cout << stoi("hi") << endl;


    return 0;
}